#include <iostream>

using namespace std;

int main()
{
    int a, b;
    cin >> a;

    int result = a;

    cin >> b;
    if (b > result)
        result = b;
    cin >> b;
    if (b > result)
        result = b;
    cin >> b;
    if (b > result)
        result = b;
    cin >> b;
    if (b > result)
        result = b;
    cin >> b;
    if (b > result)
        result = b;
    cin >> b;
    if (b > result)
        result = b;
    cin >> b;
    if (b > result)
        result = b;
    cin >> b;
    if (b > result)
        result = b;
    cin >> b;
    if (b > result)
        result = b;

    cout << result;
}

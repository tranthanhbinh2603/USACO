#include <iostream>

using namespace std;

int main()
{
    int n, a;
    cin >> n;
    int temp = 0;
    int result = 0;
    if (temp < n)
    {
        temp++;
        cin >> a;
        if (a > result) result = a;
    }
    if (temp < n)
    {
        temp++;
        cin >> a;
        if (a > result) result = a;
    }
    if (temp < n)
    {
        temp++;
        cin >> a;
        if (a > result) result = a;
    }
    if (temp < n)
    {
        temp++;
        cin >> a;
        if (a > result) result = a;
    }
    if (temp < n)
    {
        temp++;
        cin >> a;
        if (a > result) result = a;
    }
    if (temp < n)
    {
        temp++;
        cin >> a;
        if (a > result) result = a;
    }
    if (temp < n)
    {
        temp++;
        cin >> a;
        if (a > result) result = a;
    }
    if (temp < n)
    {
        temp++;
        cin >> a;
        if (a > result) result = a;
    }
    if (temp < n)
    {
        temp++;
        cin >> a;
        if (a > result) result = a;
    }
    if (temp < n)
    {
        temp++;
        cin >> a;
        if (a > result) result = a;
    }

    cout << result;
}

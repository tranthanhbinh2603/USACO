#include <cstdio>
#include <iostream>
#include <math.h>
using namespace std;

int main(){
    freopen("sqrt.inp", "r", stdin);
    freopen("sqrt.out", "w", stdout);

    int a;
    cin >> a;

    cout << sqrt(a);
}

#include <iostream>
#include <vector>
#include <queue>
using namespace std;

class Solution {
public:
    void dfs (vector<vector<int>> &gr, vector<vector<int>> &isVisited, vector<vector<int>> &starting){
        queue<pair<int, int>> q;
        for (auto i : starting)


    }
    vector<vector<int>> pacificAtlantic(vector<vector<int>>& heights) {
        int x = heights.size(); int y = heights[0].size();
        queue<pair<int, int> q;
        vector<vector<int>> isVaildPacific (x, vector<int>(y));
        for (int i = 0; i < height.size(); i++)

    }
};

int main()
{
    vector<vector<int>> k;
    k = {{1,2,2,3,5},{3,2,3,4,4},{2,4,5,3,1},{6,7,1,4,5},{5,1,1,2,4}};
    Solution s;
    vector<vector<int> ans = s.pacificAtlantic(k);
}
